import { Text, View } from 'react-native';
import React from 'react';

export const NoResults = () => (
  <View className="h-full items-center justify-center">
    <Text className='text-zinc-600'>No results</Text>
  </View>
);
